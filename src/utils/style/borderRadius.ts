export interface BorderRadius {
    bxsm: string;
    bsm: string;
    bmd: string;
    blg: string;
    bxlg: string;
    bround: string;
}

const borderRadius: BorderRadius = {
    bxsm: ".5px",
    bsm: "1px",
    bmd: "1.25px",
    blg: "1.75px",
    bxlg: "2.25px",
    bround: "50%",
};

export default borderRadius;
