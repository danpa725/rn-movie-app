export interface BorderRadius {
    bxsm: string
    bsm: string
    bmd: string
    blg: string
    bxlg: string
}

const borderRadius: BorderRadius = {
    bxsm: ".5px",
    bsm: "1px",
    bmd: "1.5px",
    blg: "2px",
    bxlg: "2.5px",
}

export default borderRadius
